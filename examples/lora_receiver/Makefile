
include $(APPDIR)/Make.defs

PROGNAME = $(CONFIG_CANSAT_APPS_LORA_RECEIVER_PROGNAME)
PRIORITY = $(CONFIG_CANSAT_APPS_LORA_RECEIVER_PRIORITY)
STACKSIZE = $(CONFIG_CANSAT_APPS_LORA_RECEIVER_STACKSIZE)
MODULE = $(CONFIG_CANSAT_APPS_LORA_RECEIVER)

MAINSRC = lora_receiver_main.c

include $(APPDIR)/Application.mk
